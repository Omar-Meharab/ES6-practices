const firstName = 'justin';
const lastName = 'timberlake';
const fullName = firstName + ' ' + lastName;
const fullName2 = `${firstName} ${lastName} is a good lad`;

const multiline = "what\n"
    + "is\n"
    + "wrong"

const multiline2 = `hey
man
sup`
console.log(multiline2);